# Candidate fdroiddata metadata for Hermes Agent Fork.
# Do not overwrite the live fdroiddata metadata with this template. Start from
# a fresh Linux checkout, merge only the new release fields, and preserve all
# prior Builds entries and maintained metadata. Review that diff, then keep the
# source commit, Binaries URL, and AllowedAPKSigningKeys aligned with the signed
# GitHub release for reproducible-build verification.

AntiFeatures:
  NonFreeNet:
    en-US: Optional online features connect to remote model catalogs and user-configured
      remote AI providers such as OpenAI, OpenRouter, Google Gemini, Anthropic, Hugging
      Face, Qwen, Z.AI, or Corr3xt.
Categories:
  - AI Chat
License: MIT
AuthorName: MobileFork
SourceCode: https://github.com/adybag14-cyber/hermes-agent
IssueTracker: https://github.com/adybag14-cyber/hermes-agent/issues
Changelog: https://github.com/adybag14-cyber/hermes-agent/releases

Name: Hermes Agent Fork
AutoName: Hermes Agent Fork
Description: |-
    Hermes Agent Fork is an independent community fork. It is not official Nous Research or Teknium software.

    The Android app uses the com.mobilefork.hermesagent application ID to make the fork status explicit and avoid confusion with the original Hermes project.

    It provides local model chat, bounded local conversation recall, voice input, resumable model downloads, and Android workflow tools for files, app launch, accessibility-assisted UI actions, notifications, settings helpers, shortcuts, widgets, and automation records.

RepoType: git
Repo: https://github.com/adybag14-cyber/hermes-agent.git
Binaries: https://github.com/adybag14-cyber/hermes-agent/releases/download/v%v/hermes-agent-android-v%v-universal.apk

Builds:
  - versionName: 0.13.147
    versionCode: 144790
    commit: REPLACE_WITH_RELEASE_COMMIT_HASH
    subdir: android/app
    sudo:
      - apt-get update
      - apt-get install -y python3-pip
    gradle:
      - yes
    prebuild:
      - sed -i -e 's/System.getenv("HERMES_RELEASE_TAG").orEmpty().trim()/"v$$VERSION$$"/'
        build.gradle.kts
      - sed -i -e 's/return if (osName.contains("windows")) "python" else "python3"/return
        if (osName.contains("windows")) "python" else "python3.13"/' build.gradle.kts

AllowedAPKSigningKeys: 2cbdb94d6081413055af1e903d4c1d6714300b4240402d1d4c8182fda777d14e

AutoUpdateMode: Version
UpdateCheckMode: Tags ^v0\.
UpdateCheckData: fdroid/com.mobilefork.hermesagent.version|versionCode=(\d+)|.|versionName=(.*)
CurrentVersion: 0.13.147
CurrentVersionCode: 144790
